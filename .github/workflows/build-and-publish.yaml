name: Build and Publish

on:
  push:
    branches: [ "main" ]
  workflow_call:
  schedule:
    - cron: '0 0 1 * *'

env:
  REGISTRY: ghcr.io

jobs:
  build-binary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: 'go.mod'
          cache: true

      - name: Build Go Binary
        run: env GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-s -w" -o gompeg ./main.go

      - name: Upload Binary Artifact
        uses: actions/upload-artifact@v7
        with:
          name: gompeg-binary
          path: gompeg
          retention-days: 14

  build-docker:
    needs: build-binary
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        variant: [scratch, nvidia]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Download Binary Artifact
        uses: actions/download-artifact@v8
        with:
          name: gompeg-binary

      # Downcase the repository name to satisfy Docker registry specifications
      - name: Lowercase Repository Name
        run: |
          echo "IMAGE_NAME=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      # Parse FFMPEG_VERSION directly from the Dockerfile
      - name: Extract FFmpeg Version from Dockerfile
        run: |
          VERSION=$(grep -E '^ARG FFMPEG_VERSION=' Dockerfile | cut -d'=' -f2)
          echo "FFMPEG_VERSION=${VERSION}" >> $GITHUB_ENV

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          build-args: |
            VARIANT=${{ matrix.variant }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.FFMPEG_VERSION }}-${{ matrix.variant }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ matrix.variant }}
